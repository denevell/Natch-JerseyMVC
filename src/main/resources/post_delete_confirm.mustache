<html>
	<head>
		<title>Delete post - Android Manchester</title>
    {{> common_header_elements}}
	</head>
  <body class="container">

  {{> nav_bar}}

  <div style="color: red;" id="error_message">
	{{errorMessage}}
  </div>

    <div>Sure you want to delete the post?</div>
    <div>There's no going back.</div>
    <form method="post" 
      action="/post/delete/{{id}}?thread={{parentThreadId}}&limit={{limit}}&start={{start}}"
      style="width:50%">
        <input 
        name="delete_post_id_form" value="{{id}}" type="hidden" /> 
        {{#loggedIn}}
	        <input type="submit" 
        class="form-control form-bottom-margin" 
	        id="delete_post_form_submit" value="Delete post? Forever?" />
        {{/loggedIn}}
        {{^loggedIn}}
	        <input type="submit" 
          class="form-control form-bottom-margin" 
	        id="delete_post_form_submit" disabled="disabled" value="you must be logged in" />
        {{/loggedIn}}
	</form>
	
	</body>
</html>